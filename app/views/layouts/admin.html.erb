<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Admin Dashboard" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css' %>
</head>
<body>
  <div class="admin-dashboard">

    <!-- Top Navigation Bar -->
    <nav class="top-bar navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Admin</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDarkDropdown" aria-controls="navbarNavDarkDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDarkDropdown">
          <ul class="navbar-nav ms-auto"> <!-- ms-auto class aligns items to the right -->
            <!-- User Info Dropdown -->
            <li class="nav-item dropdown">
              <button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                <%= current_admin_user.email %> <!-- Display current user email -->
              </button>
              <ul class="dropdown-menu dropdown-menu-dark">
                <%# <li><a class="dropdown-item" href="#">User Info</a></li>
                <li><a class="dropdown-item" href="#">Settings</a></li> %>
                <li>
                  <%= link_to "Logout", destroy_admin_user_session_path, method: :delete, class: "dropdown-item" %> <!-- Logout Link -->
                </li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    
    <!-- Main Content -->
    <main class="admin-main">
      <!-- Sidebar -->
      <nav id="sidebar" class="col-md-3 col-lg-2 d-md-block bg-light sidebar">
        <div class="sidebar-sticky">
          <ul class="nav flex-column">
            <li class="nav-item">
              <%= link_to "Dashboard", admin_root_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Users", admin_users_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Products", admin_products_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Orders", admin_orders_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Categories", admin_categories_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </nav>

      <!-- Main Content Section -->
      <div id="admin-content">
        <%= yield %> <!-- Main content area will be populated here -->
      </div>

    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white text-center">
      <p>&copy; <%= Time.now.year %> ElectroDepot</p>
    </footer>

  </div>

  <!-- Bootstrap JS Bundle (includes Popper.js) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
